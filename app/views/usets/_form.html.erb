<%= form_for(@uset) do |f| %>
  <% if @uset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uset.errors.count, "error") %> prohibited this uset from being saved:</h2>

      <ul>
      <% @uset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
